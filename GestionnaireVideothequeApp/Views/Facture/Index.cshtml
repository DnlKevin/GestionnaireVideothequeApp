@model IEnumerable<GestionnaireVideothequeApp.Models.FACTURE>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DATEFACTURE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MONTANT)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DATEFACTURE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MONTANT)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FACTUREID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FACTUREID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FACTUREID })
        </td>
    </tr>
}

</table>
